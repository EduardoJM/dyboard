import styled from 'styled-components';

const Container = styled.div`
    width: 100%;
    height: 100%;
    background: #222;
    flex: 1;
    overflow: hidden;

    position: relative;
`;

export default Container;
